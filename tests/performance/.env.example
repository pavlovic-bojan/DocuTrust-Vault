# DocuTrust Vault - Performance Test Config
# Copy to .env and set values (do not commit .env)

# Required: Backend origin (no trailing slash)
# Local: http://localhost:4000
BASE_URL=http://localhost:4000

# Optional: JWT for authenticated endpoints (health works without it)
# Seed admin: admin@doctrust.local / Admin123!
# Get via: curl -s -X POST $BASE_URL/api/v1/auth/login \
#   -H "Content-Type: application/json" \
#   -d '{"email":"admin@doctrust.local","password":"Admin123!"}' | jq -r '.token'
AUTH_TOKEN=
